{config, ...}: {
  users = {
    # Don't allow mutation of users outside the config.
    mutableUsers = false;

    groups = {
      jhc = {};
    };

    users = {
      jhc = {
        # generated by `mkpasswd -m scrypt`
        # we have to use hashed password here when using tmpfs for /
        initialHashedPassword = "$7$CU..../....c8s4IqMoRtRsI13q11KZI0$djwyCmk7V.MJW5EfAK1pJGbekV./VZfTrt2BczEYMF1";
        home = "/home/jhc";
        isNormalUser = true;
        description = "Jun-Hong Cheng";
        extraGroups = [
          "jhc"
          "users"
          "networkmanager"
          "wheel"
        ];
        openssh.authorizedKeys.keys = [
          "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKHjmk95MME81Aiz/FoCoD5JOXJtzZkz9A9qmsOmrtI/ qwe854896@Jun-Hongs-MacBook-Air.local"
        ];
      };

      # root's ssh key are mainly used for remote deployment
      root = {
        inherit (config.users.users.jhc) hashedPasswordFile;
        openssh.authorizedKeys.keys = config.users.users.jhc.openssh.authorizedKeys.keys;
      };
    };
  };
}
